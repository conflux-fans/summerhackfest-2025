{
  "name": "ownership-nft-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for NFT ownership verification platform",
  "main": "hardhat.config.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js",
    "deploy:testnet": "hardhat run scripts/deploy.js --network confluxTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network conflux",
    "verify": "hardhat verify",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@hardhat/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.17.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}