import type { ParsedCaipAddress } from '@reown/appkit-common';
export interface WalletButtonControllerState {
    data?: ParsedCaipAddress;
    error?: Error;
    ready?: boolean;
    pending: boolean;
}
export declare const WalletButtonController: {
    state: WalletButtonControllerState;
    setReady(ready: boolean): void;
    setPending(pending: boolean): void;
    setError(error?: Error): void;
    setData(data: ParsedCaipAddress): void;
};
