import type { ChainNamespace } from '@reown/appkit-common';
import type { Wallet } from './utils/TypeUtil.js';
export declare class AppKitWalletButton {
    private namespace?;
    constructor({ namespace }?: {
        namespace?: ChainNamespace;
    });
    isReady(): boolean | undefined;
    subscribeIsReady(callback: ({ isReady }: {
        isReady: boolean;
    }) => void): void;
    connect(wallet: Wallet): Promise<import("@reown/appkit-common").ParsedCaipAddress>;
    updateEmail(): Promise<{
        email: string;
    }>;
}
